export function IndexerPage(): JSX.Element {
    return (
        <section className="page-empty">
            <h1>索引器</h1>
            <p className="empty-state">尚无索引器</p>
            <p className="empty-hint">从内置目录一键添加，或导入本地 YAML 定义。</p>
        </section>
    );
}
